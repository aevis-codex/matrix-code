# 当前操作者上下文实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 TDD。按步骤勾选进度。

**目标：** 让桌面端主工作区具备当前操作者上下文，并把已有 actor 字段的操作从固定常量迁移到当前操作者。

**架构：** 只改桌面端主工作区和测试；复用第 39 阶段项目成员 API 与第 40 阶段 actor 透传 API，不新增服务端表结构。

---

## 任务 1：当前操作者加载与选择

- [x] 步骤 1：为桌面端写失败测试，断言主工作区加载项目成员并显示当前操作者选择器。
- [x] 步骤 2：在 `App` ready state 增加 `projectMembers`，刷新时加载成员。
- [x] 步骤 3：实现角色默认 actor 和手动选择持久化。
- [x] 步骤 4：运行目标测试确认通过。

## 任务 2：操作 actor 接入

- [x] 步骤 1：写失败测试，断言切换当前操作者后，提醒已读 API 发送该用户。
- [x] 步骤 2：把提醒已读、本地审批/取消、开发编码智能体和运维动作接入当前操作者。
- [x] 步骤 3：运行桌面端目标测试确认通过。

## 任务 3：回归与图谱

- [x] 步骤 1：运行桌面端全量测试和构建。
- [x] 步骤 2：运行 `git diff --check`。
- [x] 步骤 3：更新 Obsidian `MatrixCode` 图谱和阶段成果页。
