# MatrixCode 第 63 阶段计划：Agent 缓存策略与运行成本闭环

## 目标

把模型网关缓存策略从固定实现推进到角色级配置，并在运行中心按选中 Agent 运行展示模型成本与缓存命中聚合，为后续 prompt 编排器和供应商缓存优化打基础。

## 步骤

- [x] 1. 后端红测：角色配置缓存策略字段、持久化、模型请求使用角色策略。
- [x] 2. 前端红测：配置中心缓存字段保存，运行中心运行成本聚合展示。
- [x] 3. 后端实现：领域对象、命令、MyBatis-Plus/JDBC 仓储、Flyway 迁移、模型网关策略接入。
- [x] 4. 前端实现：API 类型、配置表单、运行中心聚合视图。
- [x] 5. 验证：目标测试、全量测试、构建、真实集成、密钥扫描、Git diff 检查。
- [x] 6. 第二大脑：更新项目首页、阶段索引、模块地图、技术栈与运行约定、模型网关专题和风险。
- [x] 7. 提交并推送到 `origin master`。

## 回溯检查点

- 多人实时协作智能体控制台：不变，本阶段强化每个角色智能体的独立运行配置。
- 每个角色独立工作台：不变，配置中心仍按角色标签管理。
- 真实模型与真实中间件：不写入密钥，继续读取 `.env.local`；数据库 schema 继续是 `matrix_code`。
- 可上线标准：新增 DDL 含注释，核心方法补充职责注释，测试和真实集成必须通过。
