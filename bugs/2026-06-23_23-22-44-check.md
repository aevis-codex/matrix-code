# MatrixCode 计划实现检查结果

- 复查状态：已关闭
- 关闭时间：2026-06-25 12:57:11 CST（Asia/Shanghai）
- 当前验证分支：`feature/mvp-vertical-slice`
- 当前验证结论：历史问题不再复现；原“未通过”结论作废。
- 当前验证命令：
  - `/Users/Masons/Ai/Maven/bin/mvn -Dmaven.repo.local=/Users/Masons/Ai/Maven_Ai_Store -q -pl server -Dtest=ApprovalPolicyTest test`：退出码 `0`
  - `/Users/Masons/Ai/Maven/bin/mvn -Dmaven.repo.local=/Users/Masons/Ai/Maven_Ai_Store -q -pl server test`：退出码 `0`，`271` 个测试，`0` failures，`0` errors，`0` skipped
  - `docker compose config`：退出码 `0`
  - `docker-compose config`：退出码 `0`

- 检查时间：2026-06-23 23:22:44 CST（Asia/Shanghai）
- 检查分支：`feature/mvp-vertical-slice`
- 检查提交：`5b3d506`
- 对照计划：`docs/superpowers/plans/2026-06-23-matrixcode-mvp-vertical-slice.md`
- 本次范围：已实现的任务 1–4；任务 5–12 尚未开始，不计为缺陷
- ~~结论：未通过~~

## 核对概况

任务 1–4 计划列出的 Maven 骨架、工作流、文档冻结、审批与审计文件均已落地。任务 1–3 已在计划文档中标记完成；任务 4 已提交实现，但本次验证发现以下问题。

## [已关闭] ~~删除命令可绕过人工审批门禁~~

2026-06-25 复查：当前 `ApprovalPolicyTest` 已覆盖 `rm -rf`、`rm -fr`、`rm -r -f`、多空格、大小写、`sudo`、`/bin/rm`、shell 包装和 quote 变体，测试退出码为 `0`。该缺陷当前不真实存在。

- 计划要求：删除动作必须人工确认。
- 代码位置：`server/src/main/java/com/matrixcode/approval/application/ApprovalPolicy.java:18`
- 触发代码：`action.command().contains("rm -rf")`
- 实际验证：
  - `rm  -rf /repo/payment/build` → `ALLOW`
  - `rm -fr /repo/payment/build` → `ALLOW`
- 影响：等价的递归强制删除命令可以绕过 `ASK`，直接进入自动执行路径，未满足审批计划。
- 建议方向：按解析后的命令及参数识别删除动作，不要只匹配单一字符串形式；补充空格变化、参数换序等回归测试。

## [已关闭] ~~完整服务端测试未通过~~

2026-06-25 复查：当前使用 Java 21.0.11 运行完整服务端测试，退出码为 `0`，Surefire 汇总 `271` 个测试、`0` failures、`0` errors、`0` skipped。旧的 Java 25/Mockito/ByteBuddy 问题在当前验证环境中不再复现。

- 命令：`mvn -q -pl server test`
- 结果：退出码 `1`；共 26 个测试，25 个通过，1 个错误。
- 失败测试：`MatrixCodeServerApplicationTest.应用上下文可以启动`
- 根因输出：Mockito/Byte Buddy 在 Maven 当前使用的 Java 25.0.2 下无法完成 agent self-attach，报 `Could not initialize inline Byte Buddy mock maker`。
- 相关配置：根 `pom.xml` 的 Maven Enforcer 使用 `[21,)`，因此 Java 25 被项目声明为允许版本；但当前完整测试无法在该允许版本运行。
- 环境补充：本机未安装计划指定的 Java 21，仅有 Java 17 和 Java 8；因此本次无法补做严格的 Java 21 验证。
- 隔离验证：`WorkflowServiceTest`、`DocumentServiceTest`、`ApprovalPolicyTest` 单独运行通过（退出码 `0`）。
- 建议方向：要么将构建环境明确限制为并配置 Java 21，要么修正测试运行配置，使所有被 Enforcer 接受的 JDK 版本均能通过完整测试。

## 未做事项

- 未修改任何业务代码、测试代码、构建配置或计划状态。
- 未提交、未推送。

## 2026-06-26 第 68 阶段复查

- 当前状态：继续保持已关闭。
- 复查依据：本轮 `npm --prefix desktop test`、`npm --prefix desktop run build`、`/Users/Masons/Ai/Maven/bin/mvn -Dmaven.repo.local=/Users/Masons/Ai/Maven_Ai_Store -q -pl server test`、`./scripts/check-real-runtime.sh .env.local` 和真实 `RealRuntimeIntegrationTest` 均通过。
- 结论：该文件记录的是历史阶段验收问题，当前不构成真实未完成项。
